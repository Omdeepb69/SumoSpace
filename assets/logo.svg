<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100" width="100" height="100">
  <!-- Stance / Base -->
  <rect x="20" y="55" width="60" height="30" rx="10" fill="currentColor"/>
  <!-- Head -->
  <circle cx="50" cy="30" r="18" fill="currentColor"/>
  <!-- Core / Kernel (SumoSpace Orange) -->
  <circle cx="50" cy="70" r="10" fill="#f97316"/>
</svg>
