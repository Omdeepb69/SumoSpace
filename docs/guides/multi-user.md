# Multi-User Deployment

SumoSpace isolates each session via unique `session_id` values, which map to separate ChromaDB collections.

See [Architecture — Scope Isolation](../architecture.md#scope-isolation-multi-tenancy) for details.
